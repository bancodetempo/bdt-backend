version: '3'

services:
  db:
    image: postgres
    volumes:
        - db-data:/var/lib/postgresql/data
  api:
    build: .
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
        - .env

volumes:
    db-data:
